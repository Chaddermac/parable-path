import FormationAssessment from "@/app/assessment/page";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Formation Assessment | ParablePath",
  description: "A deeper narrative reflection exploring Story Room, shadow, parable, redemptive calling, and a next faithful step.",
  alternates: { canonical: "https://parablepath.app/assessment" }
};

export default FormationAssessment;
